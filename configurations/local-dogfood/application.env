# Optional canonical production origin for background-only links. Interactive
# dogfood requests use the authenticated frontend request origin.
EMAIL_FROM=no-reply@localhost
# Optional: set to the real WorkOS email that may claim the one-time local
# super-admin bootstrap slot. Leave empty to dogfood ordinary self-serve signup.
BOOTSTRAP_ADMIN_EMAIL=
