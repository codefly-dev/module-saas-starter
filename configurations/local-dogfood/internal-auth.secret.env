# Local dogfood only. These credentials protect communication inside the
# locally started Codefly graph; external provider credentials live in the
# ignored identity.secret.env or a reference-only secret manifest.
CODEFLY_INTERNAL_TOKEN=local-dogfood-only-replace-me
CODEFLY_GATEWAY_TOKEN=local-dogfood-gateway-only-replace-me
