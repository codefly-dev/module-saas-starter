# Interactive links use the verified frontend origin propagated from Codefly's
# SDK-discovered endpoint. A background-only production origin may be supplied
# by deployment configuration; local development never pins a runtime port.
EMAIL_FROM=no-reply@localhost
BOOTSTRAP_ADMIN_EMAIL=
