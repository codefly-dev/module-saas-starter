# Canonical browser origin used in local fixture development. The frontend's
# deterministic Codefly endpoint is preserved for existing E2E workflows.
APP_BASE_URL=http://localhost:21931
EMAIL_FROM=no-reply@localhost
BOOTSTRAP_ADMIN_EMAIL=
