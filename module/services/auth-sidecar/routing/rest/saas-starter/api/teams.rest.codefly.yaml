path: /v1/organizations
routes:
    - path: /v1/organizations/{org_id}/teams
      method: POST
      extension:
        exposed: true
        protected: true
    - path: /v1/organizations/{org_id}/teams
      method: GET
      extension:
        exposed: true
        protected: true
