name: vault
version: 0.0.0
agent:
    kind: codefly:service
    name: vault
    version: 0.0.7
    publisher: codefly.dev
endpoints:
    - name: http
      api: http
      visibility: module
spec:
    transit-key: api-keys
