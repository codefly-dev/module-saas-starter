-- Canonical system-managed subscription copy is a data correction. Reverting
-- it would restore unresolved variables and a removed pricing-page contract,
-- so rollback deliberately preserves the corrected templates.
SELECT 1;
