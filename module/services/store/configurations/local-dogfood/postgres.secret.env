# Local Codefly dogfood database only. Remote environments must use a secret
# provider and unique high-entropy credentials.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_READ_ONLY_PASSWORD=local-read-only-password-change-me
POSTGRES_READ_WRITE_PASSWORD=local-read-write-password-change-me
