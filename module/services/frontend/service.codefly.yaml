name: frontend
version: 0.0.0
agent:
    kind: codefly:service
    name: nextjs
    version: 0.0.1
    publisher: codefly.dev
service-dependencies:
    - name: api
endpoints:
    - name: http
      api: http
      visibility: public
spec:
    hot-reload: true
    auth-provider: workos
    mode: ssr
