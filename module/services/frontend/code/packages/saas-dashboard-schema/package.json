{
	"name": "@codefly/saas-dashboard-schema",
	"version": "0.1.0",
	"description": "React-free data-graph schema (Event · Metric · Dashboard) that compiles declarative metrics to the audit AggregateAuditLog RPC.",
	"type": "module",
	"sideEffects": false,
	"files": [
		"dist",
		"README.md"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "vitest run --config vitest.config.ts"
	},
	"devDependencies": {
		"typescript": "5.9.3",
		"vitest": "^3.0.0"
	},
	"publishConfig": {
		"access": "public"
	},
	"license": "Apache-2.0"
}
