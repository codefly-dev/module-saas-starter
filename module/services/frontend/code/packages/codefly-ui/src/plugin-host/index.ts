/**
 * The plugin host: product-neutral React contribution composition. Re-exported
 * through the kit so the host and every Module-Federation remote import one
 * deduped copy instead of each resolving `@codefly/saas-plugin-react` on its own.
 */
export * from "@codefly/saas-plugin-react";
