// The table composite tier: data-in tables built on the layout `Table` atoms
// plus button / skeleton. Exported from `@codefly-dev/ui/table`. Composes the
// layout tier below it — never a sibling composite. React only.

export { DataTable, type DataTableProps } from "./data-table.js";
